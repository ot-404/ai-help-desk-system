services:
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=sqlite:///instance/ai_hds.db
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
    volumes:
      - ./instance:/app/instance
